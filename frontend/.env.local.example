# Copy this file to .env.local and set your local backend IP
# .env.local is gitignored — it will NOT be pushed to the repo
#
# To use:  cp .env.local.example .env.local
# Then update the IP to your machine's local IP address

EXPO_PUBLIC_API_BASE=http://YOUR_IP:8000
